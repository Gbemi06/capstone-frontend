import React from "react";
import { Button } from "react-bootstrap";

function CarouselSetting() {
  return (
    <div>
      <h1>Carousel Setting</h1>
      {/* /<Button onClick={} variant="primary">Upload Carousel</Button> */}
    </div>
  );
}

export default CarouselSetting;
